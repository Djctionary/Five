# 待实现的想法

记下来但还没做的东西。做掉一条就从这里删掉，顺手在提交信息里说一声。

## 1. 手动改时间段的颜色

现在颜色是按人分配的，`users.color_index` 在注册时定死，一个人所有的时间段都是同一个颜色。
想要的是能单独给某一段时间换颜色。

要动的地方：`availability` 加一个颜色字段（存调色板的下标，不存色值，换主题时才不会错乱），
编辑弹窗里加一排颜色可选，`components/Planner.tsx` 渲染时优先用块自己的颜色、没有再回退到人的颜色。

有个问题得先想清楚：颜色现在是「谁」的标识，日历上靠颜色认人。如果每段可以各自换色，
这个标识就没了。可能的解法是颜色仍然按人走，另外给时间段加一个标签或者图标来区分类型。
先想清楚到底是想区分人还是想区分事，再动手。

## 2. 23:00 下面加 0:00 和「彻底疯狂」

来自现代鸟语词典（09-21）：

> 你不如在 23:00 下面加一个 0:00 和彻底疯狂
>
> 选到彻底疯狂意思就是可以打到两三点

现在日历画到 24:00 就截断了，跨零点的安排没法表达。想要的是最后一格不是硬停，
而是一个「彻底疯狂」的区间，意思是能玩到凌晨两三点。

要动的地方：`lib/time.ts` 里的 `VIEW_LAST_SLOT` 现在是 48，往后扩到凌晨几点；
`availability` 的 `end_slot` 上限校验、以及 `app/actions/availability.ts` 里的范围检查都要跟着放宽。

两种实现思路：一种是老老实实把格子画到凌晨 3 点，多出来的部分在视觉上归属前一天；
另一种是最后加一个不按比例的特殊格子，标着「彻底疯狂」，选中就等于「玩到两三点」。
后者更有意思，也更符合原话，但重合计算要单独处理这一格。
